# Scratch / regenerable intermediates
.tmp/
*.npy
*.pyc
__pycache__/
.ipynb_checkpoints/

# Secrets
.env
kaggle.json
credentials.json
token.json

# Raw dataset (too large for git; regenerate via `make fetch`)
data/raw/
*.zip
twcs.csv

# OS
.DS_Store
Thumbs.db

# Virtualenvs
.venv/
venv/
env/
# Fixture/dev artifacts must never be committed as if they were real data.
# Real deliverables are force-added explicitly once generated from the
# Kaggle dump (see README "Regenerate everything from raw data").
data/threads.jsonl
data/cluster_assignments.jsonl
data/taxonomy_draft.json
data/brand_profile.csv
data/golden_unlabelled.jsonl
data/preds_*.jsonl
data/judgements.jsonl
